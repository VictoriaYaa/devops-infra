# apiVersion: v1
# kind: Service
# metadata:
#   name: alb-service
#   annotations:
#     service.beta.kubernetes.io/aws-load-balancer-type: alb
#     service.beta.kubernetes.io/aws-load-balancer-name: "alb-service"
# spec:
#   type: LoadBalancer
#   selector:
#     app: counter-service
#   ports:
#     - port: 80
#       protocol: TCP 

